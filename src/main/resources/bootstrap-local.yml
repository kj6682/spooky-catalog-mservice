message: you are connected to the item service (local)
features:
    action1: true
    action2: true
spring:
  application:
    name: items
  jackson:
    serialization:
      write_dates_as_timestamps: false
mongodb:
  uri: mongodb://localhost
  port: 27017